{% extends '::base.html.twig' %}


{% block stylesheets %}

    {{ parent() }}

    {# SPINNER BUTTONS Y MATERIAL DESIGN #}
    <link href="{{ asset('public/assets/global/css/components-md.min.css') }}" rel="stylesheet" type="text/css" />
    <link href="{{ asset('public/assets/global/css/plugins-md.min.css') }}" rel="stylesheet" type="text/css" />
    <link href="{{ asset('public/assets/global/plugins/ladda/ladda-themeless.min.css') }}" rel="stylesheet" type="text/css" />

{% endblock %}


{% block pagecontent %}
        
    {{dump(app.user)}}

	{# {{ dump(((((resultados.registros|first)|first).resultados)|first).legislacionContacto) }} #}
    {# {{ dump(resultados.registros|first) }} #}
    {# {{ dump(((resultados.registros.BIOLOGICO)|first).conclusiones) }} #}
    {# {{ dump(resultados.registros.BIOLOGICO) }} #}
    {# {{ dump(resultados.conclusiones) }} #}
    {# {{ 'es superior' in resultados.conclusiones['CAMPO'][365]['conclusionSinContacto'] }}
    {{dump('es inferiuor' in resultados.conclusiones['CAMPO'][365]['conclusionSinContacto'])}}
    {% for key, value in resultados.conclusiones %}
        {{ dump(key) }}
        {{ dump(value) }}
    {% endfor %} #}
    {# {{dump(resultados.registros.CAMPO.99)}} #}
    {{dump(resultados)}}
    {# {{ dump(((resultados.CAMPO.muestras|first).determinaciones)|first) }} #}

{% endblock %}


{% block javascripts %}

    {{ parent() }}

    {# SPINNER BUTTONS #}
    <script src="{{ asset('public/assets/global/plugins/ladda/spin.min.js') }}" type="text/javascript"></script>
    <script src="{{ asset('public/assets/global/plugins/ladda/ladda.min.js') }}" type="text/javascript"></script>
    <script src="{{ asset('public/assets/pages/scripts/ui-buttons.min.js') }}" type="text/javascript"></script>

{% endblock %}